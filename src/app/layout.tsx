import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

import './globals.css'

import { Footer, Header } from '~/components/widgets'

import { ThemeContextProvider } from '~/contexts/theme-context'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'thanhtrairo',
  description: 'Generated by thanhtrairo',
}

const RootLayout = ({
  children,
}: Readonly<{
  children: React.ReactNode
}>) => {
  return (
    <html lang="en">
      <body suppressHydrationWarning={true} className={`${inter.className} max-w-[100dvw]`}>
        <ThemeContextProvider>
          <div className="min-h-[100dvh] bg-white text-black dark:bg-black-c dark:text-white-c">
            <div className="container mx-auto px-2 sm:px-4 md:px-8 lg:px-12 xl:px-16 2xl:px-20">
              <Header />
              {children}
              <Footer />
            </div>
          </div>
        </ThemeContextProvider>
      </body>
    </html>
  )
}

export default RootLayout
